import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hiveezy.app',
  appName: 'hiveezystore',
  webDir: 'dist/freelance1',
  bundledWebRuntime: false
};

export default config;
