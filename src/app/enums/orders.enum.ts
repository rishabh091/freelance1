/**
 * Make sure the order is same as nav buttons order as each enum represents their index
 */
export enum Nav {
    NewOrder,
    CompletedOrder,
    DeliveredOrder
}

export enum StarRating {
    ZeroStar,
    OneStar,
    TwoStar,
    ThreeStar,
    FourStar,
    FiveStar
  };